# Notas

```
Opciones:
      --help     Muestra ayuda                                        [boolean]
      --version  Muestra número de versión                            [boolean]
  -b, --base     Es la base de la tabla de multiplicar      [number] [requerido]
  -l, --listar   Muestra la tabla en consola         [boolean] [default: false]
  -h, --hasta    Multiplica la base hasta el número deseado             [number]
```